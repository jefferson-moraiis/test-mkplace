import {
  errorSchema,
  inputSchema,
  inputUpdate,
  outputSuccess,
  outputSuccessString
} from './schemas/'

export default {
  error: errorSchema,
  input: inputSchema,
  inputUpdate: inputUpdate,
  outputSuccessProduct: outputSuccess,
  outputSuccessString:outputSuccessString,
}
